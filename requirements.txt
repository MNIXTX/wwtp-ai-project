# ================= 1. Core Data & Scientific Computing =================
# Verified: no np.float/np.int deprecated aliases, NumPy 2.x compatible
numpy>=1.23.0
# Verified: no DataFrame.append/ix deprecated APIs, pandas 2.3+ compatible
pandas>=1.5.0
# Verified: only solve_ivp/differential_evolution/interp1d used, stable API
scipy>=1.10.0
# Verified: only StandardScaler/mean_absolute_error/mean_squared_error used
scikit-learn>=1.2.0
joblib>=1.2.0
# Optional: try/except import, graceful degradation if missing
psutil>=5.9.0
# Optional: ODE solver JIT acceleration (asm1_ode_solver), 10-50x speedup
numba>=0.58.0

# ================= 2. Deep Learning (PyTorch Ecosystem) =================
# PyTorch: install matching your system (CPU or CUDA).
#   CPU-only:  pip install torch --index-url https://download.pytorch.org/whl/cpu
#   CUDA 11.8: pip install torch --index-url https://download.pytorch.org/whl/cu118
#   CUDA 12.4: pip install torch --index-url https://download.pytorch.org/whl/cu124
# install.bat handles this automatically.
torch>=2.0.0

# ONNX model export & inference
onnx>=1.16.0
onnxruntime>=1.14.0
# Required for PyTorch 2.x dynamo ONNX export
onnxscript>=0.1.0

# ================= 3. Machine Learning =================
lightgbm>=3.3.0

# ================= 4. Reinforcement Learning =================
# SB3 >= 2.4.0 supports gymnasium 1.0; upgrade both together
gymnasium>=0.29.0,<2.0.0
stable-baselines3>=2.4.0,<3.0.0

# ================= 5. Config & Serialization =================
PyYAML>=6.0
ruamel.yaml>=0.18.0
# Verified: V1/V2 compat layer (PYDANTIC_V2 detection), any V2 version works
pydantic>=2.0.0

# ================= 6. UI & Visualization =================
# Verified: no experimental_/beta_ deprecated APIs
streamlit>=1.30.0
matplotlib>=3.7.0

# ================= 7. Logging =================
loguru>=0.6.0

# ================= 8. API Server (Optional) =================
# Uncomment to enable FastAPI inference backend:
# fastapi>=0.110.0
# uvicorn>=0.27.0
